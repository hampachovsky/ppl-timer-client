import { Box, Typography } from '@mui/material';

const SomePage = ({}) => {
  return (
    <Box my='12px' display='flex' flexDirection='column' gap={2}>
      <Typography variant='h1'>Time Tracker</Typography>
    </Box>
  );
};

export default SomePage;
