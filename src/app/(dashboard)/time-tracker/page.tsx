import { TrackerControl } from '@/features/timeTrackers';
import { Box } from '@mui/material';

const SomePage = () => {
  return (
    <Box>
      <TrackerControl />
    </Box>
  );
};

export default SomePage;
